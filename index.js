const arr = ['Welcome', 'to', 'The', 'world'];

console.log(arr.filter((str) => str.toLowerCase().startsWith('t')));

// function to return strings in array that start with x number of cases without case sensitivity
